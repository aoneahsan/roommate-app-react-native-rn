// Enums
export enum ZFormModeE {
    edit = 'edit',
    add = 'add',
}

// Interfaces
export interface ILocation {
    country?: string
    streetAddress?: string,
    aptSuit?: string,
    city?: string,
    province?: string,
    postCode?: string,
}
